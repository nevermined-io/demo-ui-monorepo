{
  "name": "demo-ui-monorepo",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "yarn --cwd packages/server-core dev",
    "serve:app:http": "MONOREPO_UI_DIR=apps/agent-http-app yarn --cwd packages/server-core dev",
    "serve:app:mcp": "MONOREPO_UI_DIR=apps/agent-mcp-app yarn --cwd packages/server-core dev",
    "start": "NODE_ENV=production node packages/server-core/dist/index.js",
    "build": "yarn workspaces run build || true",
    "build:packages": "yarn --cwd packages/domain build && yarn --cwd packages/config build && yarn --cwd packages/agent-client build && yarn --cwd packages/prompts build && yarn --cwd packages/transport-http build && yarn --cwd packages/transport-mcp build && yarn --cwd packages/payments-client build && yarn --cwd packages/server-api build && yarn --cwd packages/ui-core build && yarn --cwd packages/server-core build",
    "build:apps": "yarn --cwd apps/demo-app build && yarn --cwd apps/agent-http-app build && yarn --cwd apps/agent-mcp-app build",
    "clean": "yarn workspaces run clean || true",
    "typecheck": "yarn workspaces run typecheck",
    "lint": "yarn workspaces run lint || true"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "@types/node": "^24.3.1",
    "@types/react": "^19.1.12",
    "@types/react-dom": "^19.1.9",
    "@vitejs/plugin-react": "^5.0.2",
    "eslint": "^9.35.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-import": "^2.32.0",
    "prettier": "^3.6.2",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "tsx": "^4.19.1",
    "typescript": "^5.9.2"
  }
}
