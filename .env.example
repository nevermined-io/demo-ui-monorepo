# Server environment variables (used by backend services)
OPENAI_API_KEY=
NVM_ENVIRONMENT=sandbox
AGENT_DID=
AGENT_ENDPOINT=
RPC_URL=

# Optional client/runtime variables (Vite)
# Prefer runtime config via public/config.js; these are fallback overrides
VITE_TRANSPORT=http
VITE_AGENT_ID=
VITE_NVM_ENVIRONMENT=sandbox
